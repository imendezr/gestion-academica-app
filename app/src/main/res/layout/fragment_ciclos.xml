<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- Buscador -->
    <androidx.appcompat.widget.SearchView
        android:id="@+id/searchViewCiclos"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_margin="6dp"
        android:background="@drawable/bg_searchview_rounded"
        android:clickable="true"
        android:elevation="2dp"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:iconifiedByDefault="false"
        android:paddingStart="12dp"
        android:paddingTop="6dp"
        android:paddingEnd="12dp"
        android:paddingBottom="6dp"
        android:queryHint="Buscar por año" />

    <!-- Contenedor para lista + ProgressBar superpuesto -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="58dp">

        <!-- Lista -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewCiclos"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:contentDescription="Listado de ciclos"
            app:layout_behavior="@string/appbar_scrolling_view_behavior" />

        <!-- ProgressBar centrado y superpuesto -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:contentDescription="Cargando ciclos"
            android:visibility="gone" />
    </FrameLayout>

    <!-- Botón flotante -->
    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
        android:id="@+id/fabCiclos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:elevation="6dp"
        android:minHeight="48dp"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:text="Agregar"
        android:textSize="16sp"
        android:tint="@android:color/white"
        app:backgroundTint="@color/colorPrimary"
        app:cornerRadius="28dp"
        app:icon="@drawable/ic_add"
        app:iconGravity="textStart"
        app:iconPadding="12dp" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>
