<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:orientation="vertical">

    <!-- Etiqueta del Spinner -->
    <TextView
        android:id="@+id/tvSpinnerLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:paddingBottom="4dp"
        android:text="@string/spinner_etiqueta"
        android:textAppearance="?attr/textAppearanceSubtitle1" />

    <!-- Contenedor del Spinner y flecha -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="@drawable/bg_spinner_clean">

        <Spinner
            android:id="@+id/spinner"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:paddingStart="12dp"
            android:paddingEnd="36dp"
            android:spinnerMode="dropdown" />

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="end|center_vertical"
            android:layout_marginEnd="8dp"
            android:clickable="false"
            android:contentDescription="@string/spinner_desplegar"
            android:focusable="false"
            android:src="@drawable/ic_arrow_drop_down" />
    </FrameLayout>
</LinearLayout>
